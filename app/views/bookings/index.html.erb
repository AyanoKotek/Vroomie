<h1>Bookings</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>User</th>
      <th>Car</th>
      <th>Date</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @bookings.each do |booking| %>
      <tr>
        <td><%= booking.id %></td>
        <td><%= booking.user.first_name %></td>
        <td><%= booking.date %></td>
        <td>
          <%= link_to "Delete", booking_path(booking), data: {turbo_method: :delete, confirm: "Are you sure you want to delete this booking?" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
